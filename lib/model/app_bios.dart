part of "model.dart";

class AppBios{
  String title;
  String tagline;
  String photoURL;

  AppBios({required this.title, required this.photoURL, required this.tagline}){
    this.title = title;
    this.tagline = tagline;
    this.photoURL = photoURL;
  }
}